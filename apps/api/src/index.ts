// Entry point for HTTP API service.
// Wire up routes, middleware, and request validation here.
// Keep transport concerns separate from domain logic in shared package.

export function startApi(): void {
  // TODO: initialize server, DI container, and route registry.
  // This placeholder makes project structure explicit for new contributors.
}
