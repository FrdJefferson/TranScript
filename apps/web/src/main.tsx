// Entry point for Web UI.
// Mount the application and configure global providers here.
// Keep UI state and API clients in separate modules as the app grows.

export function bootstrapUi(): void {
  // TODO: initialize React app and render root component.
  // Placeholder keeps the structure clear for frontend developers.
}
